# Contributing

Fork, branch, PR. Every resource needs context — explain what it is and where it fits.

```bash
pip install mkdocs-material
mkdocs serve
```

Prefer primary sources. Keep it legal. Check for duplicates.
